apiVersion: v1
kind: Service
metadata:
  name: {{ include "little-sample-cluster.fullname" . }}
  labels:
    {{- include "little-sample-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "little-sample-cluster.selectorLabels" . | nindent 4 }}
